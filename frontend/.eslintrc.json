{
  "extends": "next/core-web-vitals",
  "rules": {
    "no-restricted-syntax": [
      "error",
      {
        "selector": "CallExpression[callee.property.name=/^toLocale(String|DateString|TimeString)$/][arguments.length=0]",
        "message": "A3/H3: toLocale* with no locale uses the host's locale, so the server and the browser disagree and hydration fails. Use lib/format.js (formatDate, formatDateTime, formatDateShort, formatNumber)."
      },
      {
        "selector": "CallExpression[callee.property.name=/^toLocale(String|DateString|TimeString)$/][arguments.0.type='Identifier'][arguments.0.name='undefined']",
        "message": "A3/H3: passing `undefined` as the locale is the same as passing nothing - the host decides. Use lib/format.js."
      }
    ]
  }
}
